name: Testing PIPELINE It needs to upgraded later

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '17'

    - name: Build with Maven
      run: mvn -B clean install

  deploy:
    runs-on: ubuntu-latest

    needs: build

    steps:
    - name: Deploy to Production
      # Add your deployment steps here, such as copying the JAR file to a server, restarting services, etc.
      run: echo "Deploying to production..."

